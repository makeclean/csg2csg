class Export {
  // Constructor
  Export(std::string output) {
    SetOutput(output);
  }

  // Destructor
 ~Export();

  virtual void Export();
  
  private:
  void SetOutput(std::string output) {
   outputfile = output;
  }
   
  private:
    std::string outputfile;
}
